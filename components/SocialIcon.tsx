"use client";

import { SocialLogo } from "social-logos";
import type { SocialLogoProps } from "social-logos";

export default function SocialIcon(props: SocialLogoProps) {
  return <SocialLogo {...props} />;
}
